---
title: "Hello world"
---

This is the first post on your blog. You can delete this file from `_posts/` and add your own, or edit it to get started.

The filename format matters: `YYYY-MM-DD-title.md`. Jekyll derives the date and URL automatically from this naming convention.

## Example subheading

All standard Markdown elements (code blocks, blockquotes, lists, images) automatically match the site's typography, since the stylesheet already styles tags like `pre`, `blockquote`, and `code`.

```
console.log("example code block");
```

> This is an example blockquote.
